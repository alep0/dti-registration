[pytest]
testpaths = validations
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: mark tests that require real data and tools (deselect with -m "not integration")
